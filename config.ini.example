[base]
report_title =
input_pattern = *.xlsx
output_dir = output
intel_file = intel.csv

[network]
# 内网网段（CIDR，每行一个）
ranges =

# 内网区域关键词（逗号分隔）
internal_zones =

# 本地归属地关键词（逗号分隔）
local_geos =

# 本地公网IP（逗号分隔，支持范围简写如 1.2.3.4-10）
excluded_ips =

[health]
# 探针配置（每行一个，格式：名称|IP）
probes =
log_retention_days =

[report]
top_events =
critical_levels =
ban_levels =
