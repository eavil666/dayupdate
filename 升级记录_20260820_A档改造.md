# 升级记录 — A 档最小风险清理改造

- **日期**：2026-08-20
- **范围**：main.py / build_exe.py / release.py
- **目标**：删除死代码 + 抽取公共函数（DRY），逻辑行为零改动，编译 + 隔离回归验证
- **结论**：✅ 全部完成并验证通过

---

## 一、main.py（2512 → 2440 行，净减 72 行）

### 1. 删除死代码：`download_xdb()` 重复定义（原 844–875）
- 原文件有两个 `download_xdb()`：简单版（844–875）与 SSL 降级版（928–978）。
- Python 后定义覆盖前定义，**简单版永不执行**，属 ~32 行死代码 → 已删除。

### 2. 新增公共函数 `_safe_get()`
```python
def _safe_get(url, ssl_fallback_msg=None, log_cb=None, **kwargs):
    """requests.get 的 SSL 降级封装：先正常 verify，遇 SSL/CA 错误降级 verify=False 重试一次；
    非 SSL 异常原样上抛；kwargs 透传 requests.get。"""
```
- 统一封装 `_get_requests_verify()` + `_is_ssl_or_ca_error()` 降级逻辑。
- 保留"非 SSL 异常不降级、原样上抛"的原始行为。

### 3. 4 处 SSL 重试块收敛为 `_safe_get` 调用
| 调用点 | 降级提示消息 | 日志路由 |
|---|---|---|
| `_fetch_latest_release_api` | `[版本] GitHub API SSL验证失败，跳过证书验证重试` | `self.log_cb` |
| `_fetch_version_json` | 无（原实现即无提示） | 默认 |
| `_download` | `[更新] 下载SSL验证失败，跳过证书验证重试` | `self.log_cb` |
| `download_xdb` | `  SSL验证失败，跳过证书验证重试...` | 默认 `_log` |

> 说明：原 `download_xdb` 的"首次降级后 `verify=False` 提速"微优化未保留——每个镜像源独立走一次正常校验再降级，行为等价且更简单。

---

## 二、build_exe.py（423 → 300 行，净减 123 行）

### 4. 删除死代码：`build_updater()`（原 269–380，约 112 行）
- 从未被调用（调用早已注释），且其引用的 `updater.py` **已不存在**（updater.exe 方案因循环依赖废弃，改用 `--update-worker` 模式）。
- `import shutil` 仍被 `build_exe()` 使用，保留。

### 5. 删除失效参数 `--skip-updater` 与相关注释
- 该参数已是空操作（不再有对应调用）→ 删除，避免语义误导。

---

## 三、release.py

### 6. APP_VERSION 正则抽为共享常量
- 新增模块级 `APP_VERSION_RE`，`read_app_version` 与 `set_app_version` 共用（原两处正则不一致）。

---

## 四、验证结果

| 检查项 | 结果 |
|---|---|
| `py_compile main.py build_exe.py release.py` | ✅ OK |
| `release.py --help` / `build_exe.py --help` | ✅ 正常 |
| `_parse_version` 版本比较 | ✅ PASS |
| `_safe_get` SSL 失败 → verify=False 降级重试（2 次调用） | ✅ PASS |
| `_safe_get` 正常路径（1 次调用） | ✅ PASS |
| `_safe_get` 非 SSL 异常原样上抛（不降级） | ✅ PASS |
| `_eval_and_return` 新/旧版本判断 | ✅ PASS |
| `download_xdb` 已收敛到 `_safe_get`、无残留 `verify` 变量 | ✅ PASS |
| `download_xdb` 全文件仅 1 个定义（AST 校验） | ✅ PASS |
| release.py `read_app_version()` = 1.5.0；`APP_VERSION_RE` 读/改一致 | ✅ PASS |
| build_exe.py 无 `build_updater` / `skip-updater` 残留 | ✅ PASS |

> 回归方式：桩化 tkinter / requests 的隔离测试，不触网、不改业务数据。

---

## 五、过程中发现并修复的问题

- **自引入缺陷**：改造 `_fetch_latest_release_api` 时误删外层 `try:`，导致孤立 `except` 与悬空缩进（py_compile 报 IndentationError）。已恢复 `try:` 包裹整个请求+解析块，保留"异常 → 记录失败原因并返回 None（触发 version.json 兜底）"的原始降级行为。

---

## 六、未纳入 A 档（明确暂缓/不做）

| 项 | 原因 | 去向 |
|---|---|---|
| `github_api()` ↔ `upload_asset()` 请求/错误处理合并 | 动上传核心路径 | B 档 + 真机上传回归 |
| `log`/`log_err`/`log_warn` 三包装合并 | 收益低、改动面大 | 不做 |
| `_resolve_upx()` 抽取 | 删除 `build_updater` 后仅剩单一调用点 | YAGNI，不做 |

---

## 七、后续建议

1. 真机端到端更新测试（dist v1.4.1 → GitHub v1.5.0），确认乱码修复与更新链路。
2. 推进 B 档：更新逻辑抽独立 `updater.py` 模块 + `github_api`/`upload_asset` DRY 合并。
3. 推进 C 档：pyproject 单一版本源、pytest 套件、GitHub Actions 自动构建发布。
