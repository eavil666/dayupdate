# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.eggs/
.pytest_cache/
.coverage
.mypy_cache/
.ruff_cache/

# === 打包产物 ===
build/
dist/
*.spec
_runtime_hook.py
build_exe.spec

# === IDE / 编辑器 ===
.vscode/
.idea/
*.swp
*~

# === 运行时输出 ===
output/
*.log

# === 备份文件 ===
*.bak
*.bak_*
main.py.bak_*

# === 业务数据（敏感，不提交）===
*.xlsx
*.csv
!config.ini.example

# === 大数据库文件（运行时下载）===
*.xdb
ip2region_v4.xdb

# === 二进制工具 ===
upx.exe

# === 配置文件 ===
# config.ini 已脱敏（业务IP/探针已移至 Excel 自动读取，仅剩结构参数与内网保留段），随仓库提交

# === 环境变量文件（含 GitHub Token，敏感，不提交）===
.env
# 但提交 .env.example 模板（脱敏，供参考）
!.env.example

# === 运行时缓存（IP 归属在线查询缓存，动态数据不入库）===
geo_cache.json
threat_feeds_cache.json
# 本地威胁情报库（每日 8:30 自动刷新发布到 GitHub Release，exe 通过 --update-intel 拉取，不入 git）
threat_db.json
# tools/threat-intel 建库脚本的运行产物（data/db.json + data/raw/*），由 GitHub Actions 生成后直接发布，不入 git
tools/threat-intel/data/
# 归档的过时 demo/遗留脚本（保留参考，不入库）
tools/legacy/

# === WorkBuddy 项目记忆（本地工具数据，可能含 token 引用，不入库）===
.workbuddy/
